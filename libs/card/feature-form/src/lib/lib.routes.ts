import { Route } from '@angular/router';

import { CardFeatureFormComponent } from './card-feature-form/card-feature-form.component';

export const cardFeatureFormRoutes: Route[] = [{ path: '', component: CardFeatureFormComponent }];
